# Symfind-2 Python wrappers

This package will expose Python wrappers for the Go pipeline and analysis code.

For development, install it from the repository root with:

```sh
python -m pip install -e ./python
```
